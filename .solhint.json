{
  "extends": "solhint:recommended",
  "rules": {
    "compiler-version": ["error", "^0.8.11"],
    "func-visibility": ["warn", { "ignoreConstructors": true }],
    "max-line-warn": ["error", 100]
  }
}
